import 'package:flutter/material.dart';

class DetailBlogWidget extends StatelessWidget {
  const DetailBlogWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
